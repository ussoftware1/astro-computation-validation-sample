# Scripts

This folder is reserved for real project validation commands after codebase access.

Suggested future scripts:

- `export-reference-cases.ts` to normalize Swiss Ephemeris output into JSON fixtures.
- `compare-astrodienst-export.ts` to compare cross-validation exports.
- `run-milestone-suite.ts` to run feature-specific acceptance suites.
