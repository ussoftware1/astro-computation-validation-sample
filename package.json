{
  "name": "astro-computation-validation-evidence-pack",
  "version": "1.0.0",
  "private": true,
  "description": "NDA-safe TypeScript validation harness sample for astrology computation projects.",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.1",
    "@types/node": "^22.7.4"
  }
}
